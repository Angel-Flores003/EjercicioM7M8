CREATE TABLE MyStudents (
  id INTEGER PRIMARY KEY NOT NULL,
  text TEXT NOT NULL
);

CREATE INDEX MyStudents_full_name ON MyStudents(text);

selectAll:
SELECT * FROM MyStudents;

insert:
INSERT INTO MyStudents(text) VALUES (?);